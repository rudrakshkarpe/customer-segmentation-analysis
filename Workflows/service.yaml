apiVersion: v1
kind: Service
metadata:
  name: customer-segmentation
  labels:
    run: customer-segmentation
  namespace: retail  
spec:
  type: NodePort
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  - port: 8080
    protocol: TCP
    name: https
  selector:
    run: customer-segmentation